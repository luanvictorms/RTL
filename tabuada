function main() {
    var numero, limite;
    
    numero = 0;
    limite = 10;
    while (numero <= limite) {
        window.alert("9 x" + numero + " = " + 9 * numero);
        numero = numero + 1;
    }
}
